{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "helix-parity-contract/1.0",
  "title": "HELIX parity contract",
  "type": "object",
  "required": [
    "schema", "contract_id", "pack", "target_platform", "source_locks",
    "machine_evidence", "inputs", "expected", "comparators", "invariants"
  ],
  "properties": {
    "schema": {"type": "string", "enum": ["helix-parity-contract/1.0"]},
    "contract_id": {"type": "string"},
    "pack": {"type": "string"},
    "target_platform": {"type": "string"},
    "source_locks": {"type": "array", "items": {"type": "string"}},
    "machine_evidence": {"type": "array", "items": {"type": "string"}},
    "inputs": {"type": "object"},
    "expected": {"type": "object"},
    "comparators": {"type": "array", "items": {"type": "string"}},
    "invariants": {"type": "array", "items": {"type": "string"}}
  }
}
